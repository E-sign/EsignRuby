require "spec_helper"

describe EsignRuby do
  it "has a version number" do
    expect(EsignRuby::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
